node_modules/
__pycache__/
*.pyc
.venv/
dist/

# Local, machine-specific and never shared. These are ignored by many people's
# global gitignore, which is exactly why they belong here too: a global ignore
# is not part of the repo and does not travel with a clone.
.claude/settings.local.json
.reactor/
